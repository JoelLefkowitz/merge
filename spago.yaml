package:
  name: merge
  description: Merge sort.
  repository: https://github.com/JoelLefkowitz/merge
  homepage: https://joellefkowitz.github.io/merge
  keywords:
    - sort
    - merge
    - arrays

  publish:
    version: 0.1.1
    license: MIT
    location:
      githubOwner: joellefkowitz
      githubRepo: merge

  dependencies:
    - arrays: ">=5.0.1 <6.0.0"
    - prelude: ">=5.0.1 <6.0.0"

  test:
    main: Test.Main
    dependencies:
      - test-unit
      - debug
      - effect

  bundle:
    extra_args:
      - --minify
      - --outfile=dist/bundle.js

metadata:
  publisher: Pursuit
  languages:
    - PureScript

  frameworks: []
  paradigms:
    - Functional

  lifecycle: Alpha

workspace:
  package_set:
    registry: 41.2.0
